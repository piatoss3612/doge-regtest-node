# regtest 모드 사용
regtest=1

# 서버 모드 활성화 (RPC 서버 사용)
server=1

# RPC 사용자 이름과 비밀번호 설정 (원하는 값으로 변경)
rpcuser=myrpcuser
rpcpassword=myrpcpassword

# 모든 인터페이스에 바인딩 (또는 특정 IP 지정)
rpcbind=0.0.0.0

# RPC 접근을 허용할 IP 범위 (예: 모든 IP 허용)
rpcallowip=0.0.0.0/0

# RPC 포트 (기본값 18332를 그대로 사용하려면 생략 가능)
rpcport=18332
